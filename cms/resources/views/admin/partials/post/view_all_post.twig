{% extends 'admin/layout_admin.twig' %}
{% block main %}
    <!-- Page Heading -->
                <div class="row">
                    <div class="col-lg-12">
                        <h2 class="page-header">
                            Blog Posts
                            
                        </h2>
                         <div class="btn-group btn-breadcrumb breadcrumb">
                            <a href="{{path_for('admin.dashboard')}}" class="btn btn-default"><i class="glyphicon glyphicon-home"></i> Dashboard</a>
                            <a href="/posts" class="btn btn-info">Posts</a>
                          </div>
                    </div>
                </div>
                <!-- /.row -->
<div class="col-xs-12">
                         <table class="table table-striped table-bordered table-hover">
    <thead class="">
      <tr>
        <th>Id</th>
        <th>Title</th>
        
        <th>author</th>
        <th>published</th>
        <th>status</th>
        <th>Views</th>
        <th>Comments</th>
        <th>Category</th>
        <th>tags</th>
         <th colspan="2">Actions</th>

      </tr>
    </thead>
    <tbody>
      
      <?php foreach ($allPosts as $item): ?>
        {% for post in posts %}
      	<tr> 
        <td>{{ post.post_id }}</td>
        <td>{{ post.title }}</td>

        <td>{{ post.author }}</td>
        <td>{{ post.published }}</td>
        <td>{{ post.status }}</td>
        <td>{{ post.view_count }}</td>
        <td>{{ post.comments_count }}</td>
        <td>{{ post.cat_id }}</td>
        <td>{{ post.tags }}</td>
 
        <td><a class ="btn btn-warning" href="/post/{{ post.post_id}}/edit">Edit</a></td>
         <td><a class ="btn btn-danger confirm-delete" data-href="/post/{{ post.post_id}}/delete" data-toggle="modal" onclick="Delete.confirm(this)" >Delete</a></td>
      </tr>

      	
      {% endfor %}
       
     
    </tbody>
  </table>

  <div class="row">
    <div class="col-md-12 text-center">
      {{ paginationLinks | raw }}
      
    </div>
      
  </div>
 </div>
 {% include 'admin/includes/confirmation_modal.twig' %}
{% endblock main %}


