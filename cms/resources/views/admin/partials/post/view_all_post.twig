{% extends 'admin/layout_admin.twig' %}
{% block main %}
    <!-- Page Heading -->
                <div class="row">
                    <div class="col-lg-12">
                        <h2 class="page-header">
                            Blog Posts
                            
                        </h2>
                        <ol class="breadcrumb">
                            <li>
                                <i class="fa fa-dashboard"></i>  <a href="index.html">Dashboard</a>
                            </li>
                            <li class="active">
                                <i class="fa fa-file"></i> Blank Page
                            </li>
                        </ol>
                    </div>
                </div>
                <!-- /.row -->
<div class="col-xs-12">
                         <table class="table">
    <thead class="">
      <tr>
        <th>Id</th>
        <th>Title</th>
        
        <th>author</th>
        <th>published</th>
        <th>status</th>
        <th>Views</th>
        <th>Comments</th>
        <th>Category</th>
        <th>tags</th>

      </tr>
    </thead>
    <tbody>
      
      <?php foreach ($allPosts as $item): ?>
        {% for post in posts %}
      	<tr> 
        <td>{{ post.post_id }}</td>
        <td>{{ post.title }}</td>

        <td>{{ post.author }}</td>
        <td>{{ post.published }}</td>
        <td>{{ post.status }}</td>
        <td>{{ post.view_count }}</td>
        <td>{{ post.comments_count }}</td>
        <td>{{ post.cat_id }}</td>
        <td>{{ post.tags }}</td>
 
        <td><a class ="btn btn-warning" href="">Edit</a></td>
         <td><a class ="btn btn-danger" href="/manage/post/{{ post.post_id}}/delete">Delete</a></td>
      </tr>

      	
      {% endfor %}
       
     
    </tbody>
  </table>
 </div>
{% endblock main %}


